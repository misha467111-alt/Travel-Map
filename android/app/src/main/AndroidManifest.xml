<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Дозволи для геолокації та інтернету -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- GPS-4B2: lets the active-recording position stream keep running as
         an Android foreground service (geolocator_android's own
         GeolocatorLocationService, foregroundServiceType="location") while
         Travel Map is backgrounded or the screen is locked. Deliberately
         NOT ACCESS_BACKGROUND_LOCATION: the service is always created while
         the app is in the foreground, as a direct result of the user
         tapping Start — confirmed against current Android developer
         documentation that this is exactly the case ACCESS_BACKGROUND_LOCATION
         is NOT required for (see the GPS-4A architecture report). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- GPS-4B2.1: lets Travel Map show the recording notification in the
         drawer on Android 13+ (POST_NOTIFICATIONS). Requested lazily, only
         when a recording actually starts/resumes — never at app launch.
         A denial never blocks recording: the location foreground service
         above remains fully functional with or without it, confirmed
         against current Android developer documentation (denial only
         suppresses the notification drawer entry; the service still runs
         and is still visible in the system's foreground-service Task
         Manager either way). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:label="Travel Map"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <!-- Твій Google Maps API ключ -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
            />
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            
            <!-- Основний запуск додатку -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Фільтр для перенаправлення після авторизації Supabase OAuth -->
            <intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <!-- Змінюємо на travelmap відповідно до коду ProfileController -->
    <data
        android:scheme="io.supabase.travelmap"
        android:host="login-callback"
        android:pathPrefix="/" />
</intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
